<!DOCTYPE html>
<html lang="sv">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Ställningsplaneraren v15</title>
<style>
  :root{
    --bg:#050b16;--bg2:#081224;--panel:#0b1527;--panel2:#0e1b33;--line:#203252;--line2:#173054;
    --text:#edf4ff;--muted:#97abc7;--green:#22c55e;--green2:#16a34a;--red:#ff6b6b;--orange:#f59e0b;
    --yellow:#f8c542;--blue:#33b5ff;--chip:#0c1a31;--radius:22px;--shadow:0 18px 44px rgba(0,0,0,.35);
  }
  *{box-sizing:border-box}
  body{margin:0;font-family:Inter,Arial,Helvetica,sans-serif;background:radial-gradient(circle at top,#0c1a35 0%,var(--bg) 42%,#03070f 100%);color:var(--text)}
  .app{width:min(100%,980px);margin:0 auto;padding:18px 14px 36px}
  .hero{background:linear-gradient(180deg,rgba(15,27,50,.96) 0%,rgba(9,17,31,.98) 100%);border:1px solid var(--line);border-radius:28px;padding:18px;box-shadow:var(--shadow)}
  h1{margin:0;font-size:34px;line-height:1.05}
  .lead{margin:8px 0 0;color:var(--muted);font-size:14px}
  .tabs{display:flex;gap:8px;margin:16px 0 18px;flex-wrap:wrap}
  .tab{border:1px solid var(--line);background:linear-gradient(180deg,#0d1930 0%,#091426 100%);color:var(--text);border-radius:999px;padding:11px 15px;font-weight:700;cursor:pointer}
  .tab.active{background:linear-gradient(180deg,#16345d 0%,#102442 100%);border-color:#355c93}
  .panel{display:none}.panel.active{display:block}
  .card{background:linear-gradient(180deg,rgba(12,24,45,.95) 0%,rgba(7,16,31,.98) 100%);border:1px solid var(--line);border-radius:var(--radius);padding:16px;box-shadow:var(--shadow)}
  .title{font-size:17px;font-weight:800;margin:0 0 8px}.section{margin-top:16px}
  .grid-2,.grid-3{display:grid;gap:12px}.grid-2{grid-template-columns:1fr 1fr}.grid-3{grid-template-columns:repeat(3,1fr)}
  .input{display:flex;flex-direction:column;gap:8px}.label{font-size:13px;font-weight:700;color:#c8d6ea}
  input,select,textarea{width:100%;border-radius:14px;border:1px solid var(--line2);background:#091426;color:var(--text);min-height:46px;padding:10px 14px;font-size:16px;outline:none}
  textarea{min-height:88px;resize:vertical}
  input:focus,select:focus,textarea:focus{border-color:#35639d}
  .actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:14px}
  .btn{border:none;border-radius:14px;padding:13px 16px;font-size:15px;font-weight:800;cursor:pointer}
  .btn.primary{background:linear-gradient(180deg,var(--green) 0%,var(--green2) 100%);color:white}
  .btn.secondary{background:#101e37;color:var(--text);border:1px solid #274164}
  .helper{margin:14px 0 0;color:var(--muted);font-size:13px}.mini-note{margin-top:14px;padding:14px;border-radius:16px;border:1px solid var(--line);background:rgba(10,20,36,.6);color:#d0def1}
  .stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:16px}
  .stat{border:1px solid var(--line);background:linear-gradient(90deg,rgba(12,23,43,.95) 0%,rgba(5,15,33,.95) 100%);border-radius:20px;padding:18px;min-height:114px}
  .stat-label{color:#c1cee0;font-size:14px;margin-bottom:14px}.stat-value{font-size:27px;font-weight:900;line-height:1.05}
  .drawing-head{display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap}
  .drawing-stage{position:relative;margin-top:14px;border-radius:20px;border:1px solid var(--line);background:linear-gradient(180deg,#07111e 0%,#030914 100%);padding:14px}
  canvas{display:block;width:100%;height:auto;border-radius:14px;background:transparent}
  .overlay-btn{position:absolute;z-index:4;border:none;cursor:pointer;user-select:none;border-radius:999px;min-width:42px;min-height:42px;padding:0 14px;background:linear-gradient(180deg,#11284a 0%,#0a1830 100%);border:1px solid #30588f;color:white;font-weight:900;box-shadow:0 10px 20px rgba(0,0,0,.28)}
  .overlay-btn:hover{transform:translateY(-1px)}
  .overlay-btn.plan.plus{top:8px;left:50%;transform:translateX(-50%)}
  .overlay-btn.plan.minus{top:8px;left:calc(50% - 82px)}
  .overlay-btn.fack.plus{right:8px;top:50%;transform:translateY(-50%)}
  .overlay-btn.fack.minus{right:8px;top:calc(50% + 54px)}
  .overlay-label{position:absolute;z-index:3;color:#c8d7ea;font-size:12px;font-weight:700;letter-spacing:.02em;background:rgba(5,12,23,.72);padding:6px 10px;border-radius:999px;border:1px solid rgba(49,81,125,.6)}
  .overlay-label.top{top:58px;left:50%;transform:translateX(-50%)}.overlay-label.right{right:62px;top:50%;transform:translateY(-50%)}
  .legend{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.chip{padding:10px 12px;border-radius:999px;background:var(--chip);border:1px solid var(--line);color:#d2deef}
  .table-wrap{overflow:auto;border:1px solid var(--line);border-radius:18px;background:#071120}
  table{width:100%;min-width:700px;border-collapse:collapse} th,td{padding:14px 14px;border-bottom:1px solid #142746;text-align:left;vertical-align:top}
  th{font-size:13px;color:#c2d0e4;background:#091527;position:sticky;top:0} td.status-miss{color:var(--red);font-weight:800} td.status-ok{color:#73e79d;font-weight:800} td.status-warn{color:var(--orange);font-weight:800}
  .summary-block{margin-top:16px}.warn{color:var(--orange);font-weight:800;margin-bottom:10px}
  .article-grid{display:grid;grid-template-columns:1fr;gap:12px}
  .article-card{display:grid;grid-template-columns:70px 1fr auto;gap:12px;align-items:center;border:1px solid var(--line);border-radius:18px;padding:12px;background:linear-gradient(180deg,#0d1930 0%,#091425 100%)}
  .thumb{width:70px;height:70px;border-radius:16px;border:1px solid var(--line);background:linear-gradient(180deg,#122540 0%,#0b1525 100%);display:flex;align-items:center;justify-content:center;overflow:hidden}
  .thumb svg{width:54px;height:54px}.article-name{font-weight:900;font-size:16px}.article-meta{color:var(--muted);font-size:13px;margin-top:4px}.badge{display:inline-flex;padding:8px 10px;border-radius:999px;background:#0f203a;border:1px solid #28456f;font-weight:800}
  .rule-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:14px}
  .rule-box{padding:16px;border-radius:18px;border:1px solid var(--line);background:#091527}.rule-big{font-size:28px;font-weight:900}
  .rule-pass{color:#73e79d}.rule-warn-text{color:var(--orange)}.rule-fail{color:var(--red)}
  .checklist{display:grid;gap:10px}.note{font-size:13px;color:var(--muted)}
  .footer-note{margin-top:16px;padding:14px;border-radius:16px;border:1px solid #31527f;background:rgba(9,21,39,.75);color:#dbe7f6;line-height:1.6}
  .pill{display:inline-flex;align-items:center;gap:8px;padding:8px 10px;border-radius:999px;border:1px solid var(--line);background:#0c1730;font-size:13px;color:#d2deef}
  @media (max-width:820px){.stats,.grid-3,.rule-summary{grid-template-columns:1fr 1fr}}
  @media (max-width:640px){h1{font-size:30px}.grid-2,.stats,.grid-3,.rule-summary{grid-template-columns:1fr}.article-card{grid-template-columns:64px 1fr}.article-card .badge{grid-column:2}.overlay-btn.plan.minus{left:16px;transform:none}.overlay-btn.plan.plus{left:auto;right:16px;transform:none}.overlay-label.top{left:50%;transform:translateX(-50%)}}
</style>
</head>
<body>
<main class="app">
  <div class="hero">
    <h1>Ställningsplaneraren v15</h1>
    <p class="lead">Enfilslösning för material, lager, plocklista och preliminär regelkontroll för prefabricerad fasadställning.</p>

    <div class="tabs">
      <button class="tab active" data-tab="oversikt">Översikt</button>
      <button class="tab" data-tab="artiklar">Artiklar</button>
      <button class="tab" data-tab="lager">Lager</button>
      <button class="tab" data-tab="regelkontroll">Regelkontroll</button>
    </div>

    <section class="panel active" id="oversikt">
      <div class="card">
        <div class="grid-2">
          <div class="input" style="grid-column:1/-1">
            <label class="label" for="projectName">Projekt / plats</label>
            <input id="projectName" value="Mitt projekt" />
          </div>
          <div class="input" style="grid-column:1/-1">
            <label class="label" for="facadeLength">Fasadlängd (meter)</label>
            <input id="facadeLength" value="12,28" />
          </div>
          <div class="input">
            <label class="label" for="heightInput">Höjd (meter)</label>
            <input id="heightInput" value="8" />
          </div>
          <div class="input">
            <label class="label" for="levelsInput">Arbetsnivåer</label>
            <input id="levelsInput" value="4" />
          </div>
          <div class="input">
            <label class="label" for="stairsSelect">Trappa</label>
            <select id="stairsSelect"><option>Ja</option><option>Nej</option></select>
          </div>
          <div class="input">
            <label class="label" for="stairBaySelect">Trappfack</label>
            <select id="stairBaySelect"></select>
          </div>
          <div class="input">
            <label class="label" for="loadingClass">Lastklass</label>
            <select id="loadingClass"><option>1</option><option>2</option><option selected>3</option><option>4</option><option>5</option><option>6</option></select>
          </div>
          <div class="input">
            <label class="label" for="wallTieSelect">Visa väggfäste-påminnelse</label>
            <select id="wallTieSelect"><option>Ja</option><option>Nej</option></select>
          </div>
        </div>

        <h3 class="title section">Lager</h3>
        <div class="grid-2" id="stockInputs"></div>

        <div class="actions">
          <button class="btn primary" onclick="recalculate()">Beräkna</button>
          <button class="btn secondary" onclick="window.print()">Skriv ut / PDF</button>
          <button class="btn secondary" onclick="downloadPicklist()">Ladda ner plocklista</button>
          <button class="btn secondary" onclick="resetProject()">Nollställ projekt</button>
        </div>
        <p class="helper">V15 sparar inmatning lokalt i mobilen/datorn. Byggknapparna ligger kvar direkt på ritningen.</p>
        <div class="mini-note">Obs: Regelkontrollen är ett planeringsstöd. Den ersätter inte konstruktionsbedömning, leverantörens monteringsinstruktion eller arbetsgivarens ansvar.</div>
      </div>

      <div class="stats">
        <div class="stat"><div class="stat-label">Fack</div><div class="stat-value" id="statBays">4</div></div>
        <div class="stat"><div class="stat-label">Byggd längd</div><div class="stat-value" id="statLength">12,28 m</div></div>
        <div class="stat"><div class="stat-label">Byggd höjd</div><div class="stat-value" id="statHeight">8,00 m</div></div>
        <div class="stat"><div class="stat-label">Trappfack</div><div class="stat-value" id="statStairBay">Fack 2</div></div>
      </div>

      <div class="card section">
        <div class="drawing-head"><div><h2 style="margin:0;font-size:24px">Fasadritning</h2></div></div>
        <div class="drawing-stage">
          <button class="overlay-btn plan minus" onclick="changeLevels(-1)">− Plan</button>
          <button class="overlay-btn plan plus" onclick="changeLevels(1)">+ Plan</button>
          <span class="overlay-label top">Bygg på höjden</span>
          <button class="overlay-btn fack plus" onclick="changeBays(1)">+ Fack</button>
          <button class="overlay-btn fack minus" onclick="changeBays(-1)">− Fack</button>
          <span class="overlay-label right">Bygg på bredden</span>
          <canvas id="drawing" width="1400" height="700"></canvas>
        </div>
        <div class="legend">
          <span class="chip">Grå = ramar</span><span class="chip">Grön = plank</span><span class="chip">Vit = räcken</span><span class="chip">Blå = diagonal</span><span class="chip">Gul = trappa</span><span class="chip">Orange = väggfäste-påminnelse</span>
        </div>
      </div>

      <div class="card section">
        <h2 style="margin:0 0 14px;font-size:24px">Materiallista mot lager</h2>
        <div class="table-wrap"><table><thead><tr><th>Artikel</th><th>Behövs</th><th>Finns</th><th>Saknas</th><th>Status</th></tr></thead><tbody id="warehouseRows"></tbody></table></div>
      </div>

      <div class="card section">
        <h2 style="margin:0 0 14px;font-size:24px">Plocklista</h2>
        <div class="table-wrap"><table><thead><tr><th>Artikelnummer</th><th>Benämning</th><th>Att plocka</th></tr></thead><tbody id="pickRows"></tbody></table></div>
        <div class="summary-block">
          <h2 style="margin:0 0 10px;font-size:24px">Sammanfattning</h2>
          <div class="warn" id="summaryWarn">Du saknar delar.</div>
          <div id="summaryText" style="color:#cedaec;line-height:1.7"></div>
        </div>
      </div>
    </section>

    <section class="panel" id="artiklar">
      <div class="card">
        <h2 style="margin:0 0 8px;font-size:24px">Artiklar</h2>
        <p class="lead" style="margin-bottom:16px">Artikelbenämning med miniatyrbilder och beräknat antal utifrån ritningen.</p>
        <div class="article-grid" id="articleGrid"></div>
      </div>
    </section>

    <section class="panel" id="lager">
      <div class="card">
        <h2 style="margin:0 0 8px;font-size:24px">Lager</h2>
        <p class="lead" style="margin-bottom:16px">Egen flik för lagerstatus så den blir lätt att hitta på mobilen.</p>
        <div class="table-wrap"><table><thead><tr><th>Artikel</th><th>Benämning</th><th>Finns i lager</th><th>Behövs</th><th>Saknas</th><th>Status</th></tr></thead><tbody id="stockTabRows"></tbody></table></div>
      </div>
    </section>

    <section class="panel" id="regelkontroll">
      <div class="card">
        <h2 style="margin:0 0 8px;font-size:24px">Regelkontroll</h2>
        <p class="lead" style="margin-bottom:16px">Preliminär kontroll för att fånga vanliga avvikelser innan ställningen byggs eller tas i bruk.</p>

        <div class="grid-3">
          <div class="input">
            <label class="label" for="facadeGap">Avstånd till fasad (meter)</label>
            <input id="facadeGap" value="0,20" />
          </div>
          <div class="input">
            <label class="label" for="accessType">Tillträdesled</label>
            <select id="accessType"><option selected>Trappa</option><option>Landgång</option><option>Fast stege</option><option>Lös stege</option><option>Okänt</option></select>
          </div>
          <div class="input">
            <label class="label" for="trainingLevel">Utbildningsnivå</label>
            <select id="trainingLevel"><option>Ingen uppgift</option><option>Information / enklare arbete</option><option>Allmän ställningsutbildning</option><option>Särskild ställningsutbildning</option></select>
          </div>
          <div class="input">
            <label class="label" for="guardrailSelect">Yttre skyddsräcke finns</label>
            <select id="guardrailSelect"><option selected>Ja</option><option>Nej</option><option>Delvis</option></select>
          </div>
          <div class="input">
            <label class="label" for="insideProtection">Skydd på insida vid fasadglapp</label>
            <select id="insideProtection"><option>Ja</option><option selected>Nej</option><option>Ej aktuellt</option></select>
          </div>
          <div class="input">
            <label class="label" for="toeBoardSelect">Fotlist finns</label>
            <select id="toeBoardSelect"><option selected>Ja</option><option>Nej</option><option>Delvis</option></select>
          </div>
          <div class="input">
            <label class="label" for="foundationSelect">Underlag kontrollerat</label>
            <select id="foundationSelect"><option selected>Ja</option><option>Nej</option><option>Osäkert</option></select>
          </div>
          <div class="input">
            <label class="label" for="tieSelect">Förankring kontrollerad</label>
            <select id="tieSelect"><option selected>Ja</option><option>Nej</option><option>Osäkert</option></select>
          </div>
          <div class="input">
            <label class="label" for="planSelect">Plan för uppförande/användning/nedmontering</label>
            <select id="planSelect"><option selected>Ja</option><option>Nej</option><option>Pågår</option></select>
          </div>
          <div class="input">
            <label class="label" for="inspectionSelect">Slutkontroll dokumenterad</label>
            <select id="inspectionSelect"><option>Ja</option><option selected>Nej</option><option>Pågår</option></select>
          </div>
          <div class="input">
            <label class="label" for="componentApproval">Komponenter enligt system / typkontroll</label>
            <select id="componentApproval"><option selected>Ja</option><option>Nej</option><option>Osäkert</option></select>
          </div>
          <div class="input">
            <label class="label" for="usageState">Status</label>
            <select id="usageState"><option selected>Planering</option><option>Inför byggstart</option><option>Färdig för användning</option></select>
          </div>
        </div>

        <div class="section input">
          <label class="label" for="manualNotes">Anteckningar</label>
          <textarea id="manualNotes" placeholder="Ex. förankring enligt leverantörens montageanvisning, särskild risk vid hörn, väderskydd, annan avvikelse..."></textarea>
        </div>
      </div>

      <div class="card section">
        <div class="rule-summary">
          <div class="rule-box"><div class="note">Godkända kontroller</div><div class="rule-big rule-pass" id="rulePassCount">0</div></div>
          <div class="rule-box"><div class="note">Varningar</div><div class="rule-big rule-warn-text" id="ruleWarnCount">0</div></div>
          <div class="rule-box"><div class="note">Röda avvikelser</div><div class="rule-big rule-fail" id="ruleFailCount">0</div></div>
        </div>
        <div class="pill" id="overallStatusPill">Status: Ej beräknad</div>
        <div class="table-wrap section"><table><thead><tr><th>Kontrollpunkt</th><th>Status</th><th>Kommentar</th></tr></thead><tbody id="ruleRows"></tbody></table></div>
        <div class="footer-note" id="ruleFootnote"></div>
      </div>
    </section>
  </div>
</main>

<script>
const STORAGE_KEY = 'scaffoldPlannerV15';
const articleMeta = [
  { key:'frame', article:'AL-E203012', name:'Ram 2,00 x 0,73', unit:'st', stock:0, thumb:'frame' },
  { key:'plank', article:'AL-E491630', name:'U-stålplank 3,07', unit:'st', stock:0, thumb:'plank' },
  { key:'trapDoor', article:'ST-318330', name:'Luckplank / uppgångsplattform', unit:'st', stock:0, thumb:'trapdoor' },
  { key:'rail', article:'AL-E284330', name:'Dubbelräcke 3,07', unit:'st', stock:0, thumb:'rail' },
  { key:'diagonal', article:'AL-E284830', name:'Diagonalstag 3,07', unit:'st', stock:0, thumb:'diagonal' },
  { key:'foot', article:'AL-E511307', name:'Ställbar fot 0,70', unit:'st', stock:0, thumb:'foot' },
  { key:'stairs', article:'AL-E286230', name:'Trappa 3,07', unit:'st', stock:0, thumb:'stairs' },
  { key:'startBoom', article:'AL-E286207', name:'Startbom 0,73', unit:'st', stock:0, thumb:'startboom' },
  { key:'gableRail', article:'-', name:'Gavelräcke', unit:'st', stock:0, thumb:'gable' },
  { key:'coupler', article:'-', name:'Koppling', unit:'st', stock:0, thumb:'coupler' }
];

let bays = 4;
let levels = 4;
let bayWidthMeters = 3.07;
let levelHeightMeters = 2.0;
let stairBay = 2;

function thumbSVG(type){
  const stroke = '#d9e7f8', green = '#22c55e', yellow = '#f7c948', blue = '#33b5ff';
  switch(type){
    case 'frame': return `<svg viewBox="0 0 64 64" fill="none"><rect x="16" y="9" width="6" height="46" rx="2" fill="${stroke}"/><rect x="42" y="9" width="6" height="46" rx="2" fill="${stroke}"/><rect x="16" y="12" width="32" height="4" rx="2" fill="${stroke}"/><rect x="16" y="29" width="32" height="4" rx="2" fill="${stroke}"/><rect x="16" y="49" width="32" height="4" rx="2" fill="${stroke}"/></svg>`;
    case 'plank': return `<svg viewBox="0 0 64 64" fill="none"><rect x="8" y="25" width="48" height="6" rx="3" fill="${green}"/><rect x="8" y="35" width="48" height="6" rx="3" fill="${green}"/></svg>`;
    case 'trapdoor': return `<svg viewBox="0 0 64 64" fill="none"><rect x="10" y="20" width="44" height="24" rx="4" fill="${green}"/><rect x="30" y="20" width="4" height="24" rx="2" fill="#0c192f"/><circle cx="39" cy="32" r="2.7" fill="#f59e0b"/></svg>`;
    case 'rail': return `<svg viewBox="0 0 64 64" fill="none"><rect x="8" y="18" width="48" height="4" rx="2" fill="${stroke}"/><rect x="8" y="30" width="48" height="4" rx="2" fill="${stroke}"/><rect x="8" y="42" width="48" height="4" rx="2" fill="${stroke}"/></svg>`;
    case 'diagonal': return `<svg viewBox="0 0 64 64" fill="none"><path d="M14 50 L50 14" stroke="${blue}" stroke-width="5" stroke-linecap="round"/><path d="M16 14 L50 48" stroke="#7c8ba0" stroke-width="2" stroke-linecap="round" opacity=".35"/></svg>`;
    case 'foot': return `<svg viewBox="0 0 64 64" fill="none"><rect x="28" y="14" width="8" height="30" rx="3" fill="${stroke}"/><rect x="18" y="42" width="28" height="8" rx="4" fill="#a3b5cc"/><rect x="24" y="50" width="16" height="4" rx="2" fill="#64748b"/></svg>`;
    case 'stairs': return `<svg viewBox="0 0 64 64" fill="none"><path d="M12 48 H24 V39 H34 V30 H44 V21 H54" stroke="${yellow}" stroke-width="5" stroke-linecap="round" stroke-linejoin="round"/></svg>`;
    case 'startboom': return `<svg viewBox="0 0 64 64" fill="none"><rect x="8" y="34" width="48" height="7" rx="3.5" fill="#cbd6e4"/><rect x="18" y="23" width="6" height="18" rx="3" fill="#93a7c3"/><rect x="40" y="23" width="6" height="18" rx="3" fill="#93a7c3"/></svg>`;
    case 'gable': return `<svg viewBox="0 0 64 64" fill="none"><path d="M18 48 V18" stroke="${stroke}" stroke-width="4" stroke-linecap="round"/><path d="M46 48 V18" stroke="${stroke}" stroke-width="4" stroke-linecap="round"/><path d="M18 20 H46" stroke="${stroke}" stroke-width="4" stroke-linecap="round"/><path d="M18 34 H46" stroke="${stroke}" stroke-width="4" stroke-linecap="round"/></svg>`;
    default: return `<svg viewBox="0 0 64 64" fill="none"><circle cx="32" cy="32" r="18" fill="#d7e3f2"/><circle cx="32" cy="32" r="5" fill="#0b1527"/></svg>`;
  }
}

function byId(id){ return document.getElementById(id); }
function svNum(v){ return Number(String(v).replace(',', '.')); }
function formatSv(num, digits=2){ return Number(num).toLocaleString('sv-SE', {minimumFractionDigits:digits, maximumFractionDigits:digits}); }

function buildStockInputs(){
  const wrap = byId('stockInputs');
  wrap.innerHTML = articleMeta.map(item => `
    <div class="input"><label class="label">${item.name}</label><input type="number" min="0" step="1" value="${item.stock}" data-stock="${item.key}" /></div>`).join('');
  wrap.querySelectorAll('input[data-stock]').forEach(inp => inp.addEventListener('input', () => {
    const obj = articleMeta.find(x => x.key === inp.dataset.stock);
    obj.stock = Math.max(0, Number(inp.value || 0));
    renderAll();
  }));
}

function refreshStairBaySelect(){
  const sel = byId('stairBaySelect');
  sel.innerHTML = Array.from({length:bays}, (_,i)=>`<option value="${i+1}">Fack ${i+1}</option>`).join('');
  if(stairBay > bays) stairBay = bays;
  sel.value = String(stairBay);
}

function getNeeds(){
  const stairsOn = byId('stairsSelect').value === 'Ja';
  const frames = bays * levels * 2;
  const planks = bays * levels * 2;
  const trapDoor = stairsOn ? levels : 0;
  const rails = bays * levels * 2 + 2;
  const diagonal = Math.max(1, Math.ceil(bays / 4) * Math.max(1, Math.ceil(levels / 4)));
  const foot = (bays + 1) * 2;
  const stairs = stairsOn ? levels : 0;
  const startBoom = stairsOn ? levels : 0;
  const gableRail = 2 * levels;
  const coupler = Math.max(8, Math.ceil(bays / 4) * 4);
  return { frame:frames, plank:planks, trapDoor, rail:rails, diagonal, foot, stairs, startBoom, gableRail, coupler };
}

function recalculate(){
  const manualLength = svNum(byId('facadeLength').value);
  if(manualLength > 0) bays = Math.max(1, Math.round(manualLength / bayWidthMeters));
  const hVal = svNum(byId('heightInput').value);
  const lvlVal = Math.max(1, Number(byId('levelsInput').value || levels));
  if(hVal > 0) levelHeightMeters = hVal / lvlVal;
  levels = lvlVal;
  stairBay = Number(byId('stairBaySelect').value || 1);
  refreshStairBaySelect();
  renderAll();
}

function changeBays(delta){ bays = Math.max(1, bays + delta); refreshStairBaySelect(); renderAll(); }
function changeLevels(delta){ levels = Math.max(1, levels + delta); byId('levelsInput').value = levels; renderAll(); }

function renderStats(){
  const totalLength = bays * bayWidthMeters;
  const totalHeight = levels * levelHeightMeters;
  byId('facadeLength').value = formatSv(totalLength);
  byId('heightInput').value = totalHeight % 1 === 0 ? String(totalHeight) : formatSv(totalHeight);
  byId('levelsInput').value = levels;
  byId('statBays').textContent = bays;
  byId('statLength').textContent = `${formatSv(totalLength)} m`;
  byId('statHeight').textContent = `${formatSv(totalHeight)} m`;
  byId('statStairBay').textContent = byId('stairsSelect').value === 'Ja' ? `Fack ${stairBay}` : 'Ingen trappa';
}

function renderWarehouse(){
  const needs = getNeeds();
  byId('warehouseRows').innerHTML = articleMeta.map(item => {
    const need = needs[item.key] || 0;
    const missing = Math.max(0, need - item.stock);
    const ok = missing === 0;
    return `<tr><td>${item.article !== '-' ? `${item.article} ${item.name}` : item.name}</td><td>${need}</td><td>${item.stock}</td><td>${missing}</td><td class="${ok ? 'status-ok' : 'status-miss'}">${ok ? 'OK' : 'Saknas'}</td></tr>`;
  }).join('');

  byId('stockTabRows').innerHTML = articleMeta.map(item => {
    const need = needs[item.key] || 0;
    const missing = Math.max(0, need - item.stock);
    return `<tr><td>${item.article}</td><td>${item.name}</td><td>${item.stock}</td><td>${need}</td><td>${missing}</td><td class="${missing === 0 ? 'status-ok' : 'status-miss'}">${missing === 0 ? 'OK' : 'Saknas'}</td></tr>`;
  }).join('');

  byId('pickRows').innerHTML = articleMeta.map(item => {
    const need = needs[item.key] || 0;
    const missing = Math.max(0, need - item.stock);
    return `<tr><td>${item.article}</td><td>${item.name}</td><td>${missing}</td></tr>`;
  }).join('');

  const misses = articleMeta.map(item => {
    const need = needs[item.key] || 0;
    const missing = Math.max(0, need - item.stock);
    return missing > 0 ? `${item.article !== '-' ? item.article + ' ' : ''}${item.name}: ${missing} st` : null;
  }).filter(Boolean);
  byId('summaryWarn').textContent = misses.length ? 'Du saknar delar.' : 'Allt finns i lager.';
  byId('summaryWarn').style.color = misses.length ? 'var(--orange)' : '#73e79d';
  byId('summaryText').innerHTML = misses.length ? misses.join('<br>') : 'Ingen plockning behövs.';
}

function renderArticles(){
  const needs = getNeeds();
  byId('articleGrid').innerHTML = articleMeta.map(item => `
    <div class="article-card">
      <div class="thumb">${thumbSVG(item.thumb)}</div>
      <div>
        <div class="article-name">${item.name}</div>
        <div class="article-meta">Artikel: ${item.article}</div>
        <div class="article-meta">Lager: ${item.stock} st</div>
      </div>
      <div class="badge">Behövs: ${needs[item.key] || 0}</div>
    </div>`).join('');
}

function drawScaffold(){
  const canvas = byId('drawing'); const ctx = canvas.getContext('2d'); ctx.clearRect(0,0,canvas.width,canvas.height);
  const W = canvas.width, H = canvas.height; ctx.fillStyle = '#05101d'; ctx.fillRect(0,0,W,H);
  const left = 90, right = 120, top = 80, bottom = 98; const width = W - left - right; const height = H - top - bottom;
  const bayW = width / bays; const levelH = height / levels; const stairsOn = byId('stairsSelect').value === 'Ja'; const wallTieOn = byId('wallTieSelect').value === 'Ja';

  ctx.strokeStyle = '#3a4c67'; ctx.lineWidth = 4;
  for(let i=0;i<=bays;i++){ const x = left + i * bayW; ctx.beginPath(); ctx.moveTo(x, top); ctx.lineTo(x, top + height); ctx.stroke(); }

  for(let j=0;j<=levels;j++){ const y = top + j * levelH; ctx.strokeStyle = '#b9c8d8'; ctx.lineWidth = 3; ctx.beginPath(); ctx.moveTo(left - 6, y); ctx.lineTo(left + width + 6, y); ctx.stroke(); }

  for(let j=0;j<levels;j++){
    const yTop = top + j * levelH; const plankY = yTop + levelH * 0.62;
    for(let i=0;i<bays;i++){
      const x = left + i * bayW + bayW * 0.06; const plankW = bayW * 0.4;
      ctx.strokeStyle = '#22c55e'; ctx.lineWidth = 9; ctx.beginPath(); ctx.moveTo(x, plankY); ctx.lineTo(x + plankW, plankY); ctx.stroke(); ctx.beginPath(); ctx.moveTo(x + bayW * 0.46, plankY); ctx.lineTo(x + bayW * 0.46 + plankW, plankY); ctx.stroke();
      if(wallTieOn && j < levels - 1 && i % 2 === 0){ ctx.strokeStyle = '#f59e0b'; ctx.lineWidth = 5; ctx.beginPath(); ctx.moveTo(x + bayW * 0.36, plankY - 6); ctx.lineTo(x + bayW * 0.36 + 12, plankY - 6); ctx.stroke(); }
    }
  }

  ctx.strokeStyle = '#f1f5f9'; ctx.lineWidth = 4;
  for(let j=0;j<levels;j++){
    const y1 = top + j * levelH + levelH * 0.20; const y2 = top + j * levelH + levelH * 0.42;
    ctx.beginPath(); ctx.moveTo(left, y1); ctx.lineTo(left + width, y1); ctx.stroke();
    ctx.beginPath(); ctx.moveTo(left, y2); ctx.lineTo(left + width, y2); ctx.stroke();
  }

  ctx.strokeStyle = '#33b5ff'; ctx.lineWidth = 5;
  for(let i=0;i<bays;i+=4){
    const x1 = left + i * bayW; const x2 = left + Math.min(i+1,bays) * bayW;
    ctx.beginPath(); ctx.moveTo(x1, top + height); ctx.lineTo(x2, top + height - Math.min(levelH * 1.3, height)); ctx.stroke();
  }

  if(stairsOn){
    const i = stairBay - 1; const x = left + i * bayW + bayW * 0.1; const x2 = left + (i+1) * bayW - bayW * 0.12; const yBase = top + height - 10; const yTop = top + 10;
    ctx.strokeStyle = '#f8c542'; ctx.lineWidth = 6; ctx.beginPath(); ctx.moveTo(x + 20, yBase); ctx.lineTo(x2 - 10, yTop + 30); ctx.stroke();
    for(let j=0;j<levels;j++){ const yy = yBase - (j + 0.45) * levelH; ctx.beginPath(); ctx.moveTo(x + 34, yy); ctx.lineTo(x + 86, yy); ctx.stroke(); }
  }

  ctx.strokeStyle = '#8aa2c0'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(left - 34, top); ctx.lineTo(left - 34, top + height); ctx.stroke();
  ctx.fillStyle = '#d5e1f0'; ctx.font = 'bold 18px Arial'; ctx.fillText('Fasad', left - 58, top - 12);
  ctx.strokeStyle = '#aebfd3'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(left, top + height + 44); ctx.lineTo(left + width, top + height + 44); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(left, top + height + 34); ctx.lineTo(left, top + height + 54); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(left + width, top + height + 34); ctx.lineTo(left + width, top + height + 54); ctx.stroke();
  ctx.fillStyle = '#d7e3f2'; ctx.font = 'bold 17px Arial'; ctx.fillText(`${formatSv(bays * bayWidthMeters)} m`, left + width / 2 - 34, top + height + 30);

  for(let i=0;i<bays;i++){
    const x = left + i * bayW; ctx.fillStyle = '#d7e3f2'; ctx.font = '14px Arial'; ctx.fillText(`Fack ${i+1}`, x + bayW * 0.30, top + height + 14);
    ctx.beginPath(); ctx.moveTo(x + bayW * 0.5, top + height + 8); ctx.lineTo(x + bayW * 0.45, top + height + 26); ctx.lineTo(x + bayW * 0.55, top + height + 26); ctx.closePath(); ctx.fill();
  }
}

function pushRule(arr, label, status, comment){ arr.push({label, status, comment}); }
function statusClass(status){ return status === 'OK' ? 'status-ok' : status === 'Varning' ? 'status-warn' : 'status-miss'; }

function renderRules(){
  const rules = [];
  const totalHeight = levels * levelHeightMeters;
  const facadeGap = svNum(byId('facadeGap').value);
  const accessType = byId('accessType').value;
  const trainingLevel = byId('trainingLevel').value;
  const guardrail = byId('guardrailSelect').value;
  const insideProtection = byId('insideProtection').value;
  const toeBoard = byId('toeBoardSelect').value;
  const foundation = byId('foundationSelect').value;
  const tie = byId('tieSelect').value;
  const plan = byId('planSelect').value;
  const inspection = byId('inspectionSelect').value;
  const components = byId('componentApproval').value;
  const usageState = byId('usageState').value;
  const stairsOn = byId('stairsSelect').value === 'Ja';

  if(totalHeight >= 2 && guardrail !== 'Ja') pushRule(rules, 'Skyddsräcke vid fallrisk ≥ 2 m', 'Avvikelse', 'Höjden är minst 2 meter men yttre skyddsräcke är inte markerat som komplett.');
  else pushRule(rules, 'Skyddsräcke vid fallrisk ≥ 2 m', 'OK', totalHeight >= 2 ? 'Skyddsräcke markerat som installerat.' : 'Fallhöjden är under 2 meter i denna planering.');

  if(totalHeight >= 2 && toeBoard !== 'Ja') pushRule(rules, 'Fotlist där skyddsräcke krävs', toeBoard === 'Delvis' ? 'Varning' : 'Avvikelse', 'Fotlist bör vara klar på arbetsplan där skyddsräcke krävs.');
  else pushRule(rules, 'Fotlist där skyddsräcke krävs', 'OK', 'Fotlist är markerad som klar eller inte aktuell.');

  if(facadeGap > 0.30 && insideProtection !== 'Ja') pushRule(rules, 'Avstånd till fasad ≤ 0,30 m eller skydd på insida', 'Avvikelse', `Angivet fasadavstånd är ${formatSv(facadeGap)} m och inget invändigt skydd är markerat.`);
  else if(facadeGap > 0.30) pushRule(rules, 'Avstånd till fasad ≤ 0,30 m eller skydd på insida', 'Varning', `Fasadavstånd ${formatSv(facadeGap)} m kräver att invändigt skydd/konsol verkligen är korrekt utfört.`);
  else pushRule(rules, 'Avstånd till fasad ≤ 0,30 m eller skydd på insida', 'OK', `Fasadavstånd ${formatSv(facadeGap)} m ligger inom normalt riktvärde.`);

  if(accessType === 'Lös stege') pushRule(rules, 'Säker tillträdesled', 'Avvikelse', 'Lös stege bör inte användas som tillträdesled till denna typ av ställning.');
  else if((bays >= 2 || totalHeight > 2) && !stairsOn && accessType !== 'Trappa' && accessType !== 'Landgång') pushRule(rules, 'Säker tillträdesled', 'Varning', 'För flera fack eller högre ställning bör säker tillträdesled vara tydligt vald.');
  else if(accessType === 'Okänt') pushRule(rules, 'Säker tillträdesled', 'Varning', 'Tillträdesled är inte fastställd.');
  else pushRule(rules, 'Säker tillträdesled', 'OK', `Vald tillträdesled: ${accessType}${stairsOn ? ' och trappfack markerat i ritningen.' : '.'}`);

  if(foundation === 'Nej') pushRule(rules, 'Underlag kontrollerat', 'Avvikelse', 'Underlaget måste vara kontrollerat innan uppförande.');
  else if(foundation === 'Osäkert') pushRule(rules, 'Underlag kontrollerat', 'Varning', 'Underlag behöver verifieras.');
  else pushRule(rules, 'Underlag kontrollerat', 'OK', 'Underlag är markerat som kontrollerat.');

  if(tie === 'Nej') pushRule(rules, 'Förankring kontrollerad', 'Avvikelse', 'Förankring saknas eller är inte godkänd i planen.');
  else if(tie === 'Osäkert') pushRule(rules, 'Förankring kontrollerad', 'Varning', 'Förankring behöver verifieras mot system och montageanvisning.');
  else pushRule(rules, 'Förankring kontrollerad', 'OK', 'Förankring är markerad som kontrollerad.');

  if(plan === 'Nej') pushRule(rules, 'Plan för uppförande, användning och nedmontering', 'Avvikelse', 'Plan saknas.');
  else if(plan === 'Pågår') pushRule(rules, 'Plan för uppförande, användning och nedmontering', 'Varning', 'Planen är inte färdig ännu.');
  else pushRule(rules, 'Plan för uppförande, användning och nedmontering', 'OK', 'Plan finns markerad.');

  if(usageState === 'Färdig för användning' && inspection !== 'Ja') pushRule(rules, 'Slutkontroll dokumenterad innan användning', 'Avvikelse', 'Ställningen är markerad som färdig för användning men slutkontrollen är inte klar.');
  else if(inspection === 'Pågår') pushRule(rules, 'Slutkontroll dokumenterad innan användning', 'Varning', 'Kontrollen pågår men är inte färdig.');
  else if(inspection === 'Nej') pushRule(rules, 'Slutkontroll dokumenterad innan användning', usageState === 'Planering' ? 'Varning' : 'Avvikelse', 'Slutkontroll är inte dokumenterad ännu.');
  else pushRule(rules, 'Slutkontroll dokumenterad innan användning', 'OK', 'Slutkontroll är markerad som dokumenterad.');

  if(totalHeight > 9 && trainingLevel !== 'Särskild ställningsutbildning') pushRule(rules, 'Utbildningsnivå för högre/mer avancerad ställning', 'Avvikelse', `Planerad höjd är ${formatSv(totalHeight)} m. Det brukar kräva särskild ställningsutbildning.`);
  else if(totalHeight > 2 && totalHeight <= 9 && !['Allmän ställningsutbildning','Särskild ställningsutbildning'].includes(trainingLevel)) pushRule(rules, 'Utbildningsnivå för ställningsarbete', 'Varning', `Planerad höjd är ${formatSv(totalHeight)} m. Kontrollera att utbildningsnivån räcker.`);
  else pushRule(rules, 'Utbildningsnivå för ställningsarbete', 'OK', `Vald utbildningsnivå: ${trainingLevel}.`);

  if(components === 'Nej') pushRule(rules, 'Komponenter enligt system / typkontroll', 'Avvikelse', 'Komponenter är inte markerade som systemgodkända eller typkontrollerade.');
  else if(components === 'Osäkert') pushRule(rules, 'Komponenter enligt system / typkontroll', 'Varning', 'Verifiera komponenter mot leverantörens dokumentation.');
  else pushRule(rules, 'Komponenter enligt system / typkontroll', 'OK', 'Komponenter är markerade som rätt system / typkontroll.');

  const needs = getNeeds();
  const missingTotal = articleMeta.reduce((sum, item) => sum + Math.max(0, (needs[item.key]||0) - item.stock), 0);
  if(missingTotal > 0) pushRule(rules, 'Material finns i lager eller plocklista är klar', 'Varning', `Det saknas totalt ${missingTotal} delar enligt lagerjämförelsen.`);
  else pushRule(rules, 'Material finns i lager eller plocklista är klar', 'OK', 'Inga materialbrister upptäckta i lagerjämförelsen.');

  const pass = rules.filter(r => r.status === 'OK').length;
  const warn = rules.filter(r => r.status === 'Varning').length;
  const fail = rules.filter(r => r.status === 'Avvikelse').length;
  byId('rulePassCount').textContent = pass;
  byId('ruleWarnCount').textContent = warn;
  byId('ruleFailCount').textContent = fail;
  const pill = byId('overallStatusPill');
  if(fail > 0){ pill.textContent = 'Status: Ej klar – röda avvikelser finns'; pill.style.borderColor = '#7f2b2b'; pill.style.color = '#ffd2d2'; }
  else if(warn > 0){ pill.textContent = 'Status: Klar med varningar – kontrollera manuellt'; pill.style.borderColor = '#8a5e1c'; pill.style.color = '#ffe5b3'; }
  else { pill.textContent = 'Status: Preliminärt grön'; pill.style.borderColor = '#215f34'; pill.style.color = '#baf7cb'; }

  byId('ruleRows').innerHTML = rules.map(r => `<tr><td>${r.label}</td><td class="${statusClass(r.status)}">${r.status}</td><td>${r.comment}</td></tr>`).join('');
  byId('ruleFootnote').innerHTML = `Den här kontrollen är ett hjälpmedel för prefabricerad fasadställning och bygger på dina inmatade uppgifter. Verifiera alltid mot leverantörens montageanvisning, faktisk last, förankringsbild, riskbedömning och slutkontroll innan användning.`;
}

function renderAll(){ renderStats(); renderWarehouse(); renderArticles(); drawScaffold(); renderRules(); saveState(); }

function downloadPicklist(){
  const needs = getNeeds();
  const rows = articleMeta.map(item => `${item.article}\t${item.name}\t${Math.max(0, (needs[item.key] || 0) - item.stock)}`).join('\n');
  const text = 'Artikel\tBenämning\tAtt plocka\n' + rows;
  const blob = new Blob([text], {type:'text/tab-separated-values;charset=utf-8'}); const a = document.createElement('a');
  a.href = URL.createObjectURL(blob); a.download = 'plocklista_v15.tsv'; a.click(); URL.revokeObjectURL(a.href);
}

function gatherState(){
  return {
    bays, levels, bayWidthMeters, levelHeightMeters, stairBay,
    fields: Array.from(document.querySelectorAll('input, select, textarea')).reduce((acc, el) => { if(el.id) acc[el.id] = el.value; return acc; }, {}),
    stock: Object.fromEntries(articleMeta.map(item => [item.key, item.stock]))
  };
}
function saveState(){ localStorage.setItem(STORAGE_KEY, JSON.stringify(gatherState())); }
function loadState(){
  try{
    const raw = localStorage.getItem(STORAGE_KEY); if(!raw) return;
    const state = JSON.parse(raw);
    bays = state.bays ?? bays; levels = state.levels ?? levels; bayWidthMeters = state.bayWidthMeters ?? bayWidthMeters; levelHeightMeters = state.levelHeightMeters ?? levelHeightMeters; stairBay = state.stairBay ?? stairBay;
    articleMeta.forEach(item => { if(state.stock && state.stock[item.key] != null) item.stock = Number(state.stock[item.key]); });
    buildStockInputs(); refreshStairBaySelect();
    if(state.fields){ Object.entries(state.fields).forEach(([id, value]) => { const el = byId(id); if(el) el.value = value; }); }
  }catch(e){ console.warn('Kunde inte läsa sparad data', e); }
}
function resetProject(){
  localStorage.removeItem(STORAGE_KEY); location.reload();
}

document.querySelectorAll('.tab').forEach(btn => btn.addEventListener('click', () => {
  document.querySelectorAll('.tab').forEach(b => b.classList.toggle('active', b === btn));
  const tabId = btn.dataset.tab; document.querySelectorAll('.panel').forEach(p => p.classList.toggle('active', p.id === tabId));
}));

document.addEventListener('input', (e) => {
  if(e.target.matches('input, select, textarea')){
    if(e.target.id === 'stairBaySelect') stairBay = Number(e.target.value || 1);
    renderAll();
  }
});

buildStockInputs();
refreshStairBaySelect();
loadState();
refreshStairBaySelect();
renderAll();
</script>
</body>
</html>
